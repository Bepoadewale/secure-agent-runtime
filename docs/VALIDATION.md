# Validation

Run `make test lint demo demo-security`. Before marking Docker containment executed, run a fixture task and inspect effective container configuration plus negative egress/host-access tests. Record Docker version and exact results.
