# Demo scenarios

A normal test task creates/events/artifacts/destroys. B malicious repository/capability prompt is rejected. C a fake bounded timeout records `TASK_TIMEOUT` and destroys. D authorized short-lived demo secret is redacted/revoked. E default no-egress stops arbitrary destinations. F timeout/cancel paths revoke/destroy. G tenant-scoped API prevents cross-tenant retrieval. H untrusted repo requesting standard runtime is rejected rather than downgraded.
