# Capabilities

Capabilities include filesystem access, process execution, public clone, package install, approved network, artifact upload, and narrowly scoped secrets. `git.push` is denied by default. A profile centrally defines the maximum set; request intersection never escalates access. A prompt is not a permission grant.
